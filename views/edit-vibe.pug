extends layout

block content
  h2 Edit Vibe

  form(action=`/edit-vibe/${vibe._id}` method="POST")
    label Vibe Name:
    input(type="text", name="vibeName", value=vibe.vibeName, required)

    br
    button(type="submit") Update

  br
  a(href="/dashboard") Back to Dashboard